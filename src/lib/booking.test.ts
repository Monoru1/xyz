import{describe,expect,it}from"vitest";import{addMinutes}from"date-fns";describe("booking intervals",()=>{it("treats adjacent appointments as non-overlapping",()=>{const firstEnd=addMinutes(new Date("2030-01-01T10:00:00Z"),60);const secondStart=new Date("2030-01-01T11:00:00Z");expect(firstEnd<=secondStart).toBe(true)});it("detects a real overlap",()=>{const start=new Date("2030-01-01T10:30:00Z"),existingStart=new Date("2030-01-01T10:00:00Z"),existingEnd=new Date("2030-01-01T11:00:00Z");expect(existingStart<addMinutes(start,60)&&existingEnd>start).toBe(true)})});
