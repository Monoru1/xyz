import{AuthError}from"next-auth";import{signIn}from"@/auth";export default function Login(){async function login(data:FormData){"use server";try{await signIn("credentials",{email:data.get("email"),password:data.get("password"),redirectTo:"/dashboard"})}catch(e){if(e instanceof AuthError)return;throw e}}return <main className="container"><form action={login} className="card"><p className="eyebrow">ESPACE PROPRIÉTAIRE</p><h1>Connexion</h1><label>E-mail</label><input name="email" type="email" required/><label>Mot de passe</label><input name="password" type="password" minLength={8} required/><button className="button">Se connecter</button></form></main>}
