import{notFound}from"next/navigation";import{db}from"@/lib/db";import{BookingForm}from"@/components/booking-form";import{formatAmount}from"@/lib/format";export const dynamic="force-dynamic";export default async function SalonPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const salon=await db.salon.findUnique({where:{slug},include:{services:{where:{active:true},orderBy:{priceCents:"asc"}}}});if(!salon)notFound();return <main className="container"><p className="eyebrow">SALON PARTENAIRE</p><h1>{salon.name}</h1><p>{salon.description}</p><div className="grid">{salon.services.map(s=><article className="card" key={s.id}><h2>{s.name}</h2><p>{s.description}</p><p>{s.durationMinutes} min</p><b className="price">{formatAmount(s.priceCents)}</b></article>)}</div><BookingForm salonId={salon.id} services={salon.services}/></main>}
