import{redirect}from"next/navigation";import{auth}from"@/auth";import{db}from"@/lib/db";export const dynamic="force-dynamic";export default async function Dashboard(){const session=await auth();if(session?.user.role!=="OWNER")redirect("/connexion");const salon=await db.salon.findUnique({where:{ownerId:session.user.id},include:{services:true,bookings:{include:{service:true},orderBy:{startsAt:"asc"},take:30}}});if(!salon)return <main className="container">Salon non configuré.</main>;return <main className="container"><p className="eyebrow">TABLEAU DE BORD</p><h1>{salon.name}</h1><section className="grid"><article className="card"><b className="price">{salon.bookings.filter(b=>b.status==="CONFIRMED").length}</b><p>réservations confirmées</p></article><article className="card"><b className="price">{salon.services.length}</b><p>prestations</p></article></section><h2>Prochains rendez-vous</h2>{salon.bookings.map(b=><article className="card" key={b.id}><b>{b.customerName}</b> · {b.service.name}<p>{b.startsAt.toLocaleString("fr-FR")} — {b.status}</p></article>)}</main>}
